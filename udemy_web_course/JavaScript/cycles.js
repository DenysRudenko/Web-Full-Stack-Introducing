var cycle = 1;

while (cycle < 10) {
    console.log(cycle);
    cycle++;
}

alert("Loop is finished");

var string = "Hello JavaScript";
var count = 0;

while (count < string.length) {
    console.log(string[count]);
    count++;
}